<%= form_for(@home) do |f| %>
  <% if @home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home.errors.count, "error") %> prohibited this home from being saved:</h2>

      <ul>
      <% @home.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

What is your first name?<br>
<%= f.text_field :name %><br>

What is the price of the home you are buying?<br>
<%= f.text_field :asking_price %><br>

How much is the seller's concession? If unsure, leave blank.<br>
<%= f.text_field :seller_con %><br>

What is the interest rate?<br>
<%= f.text_field :rate %><br>

What are the estimated annual Real Estate Taxes?<br>
<%= f.text_field :re_taxes %><br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
